#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int P=1000 , T=2 ;
    float R = 10;
    scanf("%d %f %d", &P ,&R ,&T);
    double amount , CI;
    amount = P * pow((1 + R / 100), T);
    CI = amount - P;
    printf("%.2f", CI);
    return 0;
}
