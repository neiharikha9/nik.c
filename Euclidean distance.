#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    float x1=0 , y1=0 , x2=3 , y2=4;
    float distance;
    distance = sqrt(pow(x2 - x1, 2) + pow(y2 - y1, 2));
    printf("%.2f",distance);
    
    return 0;
}
