int main() {
    int N, t1 = 0, t2 = 1, nextTerm;

    scanf("%d", &N);

    for (int i = 1; i <= N; ++i) {
        if (i == 1)
            printf("%d", t1);
        else if (i == 2)
            printf(" %d", t2);
        else {
            nextTerm = t1 + t2;
            printf(" %d", nextTerm);
            t1 = t2;
            t2 = nextTerm;
        }
    }
    printf("\n");
    return 0;
}
